<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.igi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/igi_ic_app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/igi_ic_app"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Problem"></activity>
        <activity android:name=".Solution"></activity>
        <activity android:name=".Rate"></activity>
        <activity android:name=".Develop"></activity>
        <activity android:name=".HomeScreen" />
        <activity android:name=".signup" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>